<Window x:Class="SqlSaucey.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlSaucey.Views"
        xmlns:consoleControl="clr-namespace:ConsoleControl;assembly=ConsoleControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" MinHeight="400" MinWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <local:Menu x:Name="menu" Grid.Column="0" Grid.Row="0"  DataContext="{Binding Menu}"></local:Menu>
        <local:Setups x:Name="setups" Grid.Column="0" Grid.Row="1" DataContext="{Binding Setups}"></local:Setups>
        <Grid Margin="2" Grid.Row="2" Grid.Column="0">
            <consoleControl:Console x:Name="console" DataContext="{Binding Console}" Background="Black"></consoleControl:Console>
        </Grid>
    </Grid>
</Window>
